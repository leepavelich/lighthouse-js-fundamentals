const calculateRectangleArea = (l, w) => {
  if (length > 0 && width > 0) return l * w;
};

const calculateTriangleArea = (b, h) => {
  if (base > 0 && height > 0) return b * h / 2;
};


function calculateCircleArea(r) {
  if (radius > 0) return Math.PI * r * r;
}